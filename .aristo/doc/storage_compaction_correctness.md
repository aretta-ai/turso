**Aristo verified intent — `storage_compaction_correctness`**

A compaction operation preserves logical content, preserves all schema and engine metadata, does not increase physical footprint, and is atomic under crash.

<sub>Verify level: **neural**</sub>

---
