**Aristo verified intent — `aristos:wal_nbackfills_orders_with_recovery`**

The nbackfills counter advances after frames are durable, so recovery never replays already-checkpointed frames


<sub>Verify level: **full** · Server-bound (`aristos:` namespace)</sub>

---
